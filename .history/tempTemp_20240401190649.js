function pow(x, n) {
    let result = 1;

    // умножаем result на x n раз в цикле
    for (let i = 0; i < n; i++) {
        result *= x;
        console.log(res);
    }

    return result;
}

console.log(pow(2,3));
